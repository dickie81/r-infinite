\documentclass[11pt,a4paper]{article}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{cascade-xref}        % cross-paper \xref machinery (incl. xr-hyper)
\usepackage{booktabs}
\usepackage{graphicx}
\xrhyperdoc{prelude}{cascade-series-prelude}
\xrhyperdoc{part0}{cascade-series-part0}
\xrhyperdoc{part23}{cascade-series-part2-equals-3}
\xrhyperdoc{part3}{cascade-series-part3}
\geometry{margin=2.5cm}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{observation}[theorem]{Observation}

\title{The Cascade Series --- Part 0a\\[0.5em]
\large How Primes Fill Spacetime: Bridge between Prelude and Part 0}
\author{RTAC}
\date{May 2026}

\begin{document}
\setlength{\emergencystretch}{3em}
\maketitle
\phantomsection
\label{paperdoc}

\begin{center}
\fbox{\parbox{0.9\textwidth}{\textbf{Status.} Structural / bridging
paper. The Prelude derives $\pi$ from primes via the Euler product
and establishes the dimension tower $\{(\mathbb{R}^d, B^d)\}_{d \in
\mathbb{N}}$; Part~0 derives the four distinguished dimensions
$\{5, 7, 19, 217\}$ and the cascade invariant $\sim 10^{-120}$ from
the Gamma-function structure on integer arguments. Between them sits
a question this paper takes up: \emph{how does the prime structure
express itself across dimensions before the distinguished-dimension
machinery of Part~0 weighs in?} The paper introduces the
prime-cascade reading of the integer cascade --- atomic ticks
(primes) and derived ticks (composites), with back-references through
the divisibility lattice --- and identifies a natural divisor-weighted
quadratic invariant $\Sigma(N) = \sum_{n \leq N} \sigma(n)
\sim (\pi^2/12) N^2$. At the current cosmic depth $N \sim 10^{60}$,
$\Sigma(N) \approx 0.82 \times 10^{120}$ --- agreeing within
order-unity $\pi$-rational factors with two other independently-
derived cascade scales: Paper~0's inverse cosmological constant
$\Lambda^{-1} \approx 10^{120}$ (from $\Omega_{19} \Omega_{217}$),
and Paper~II\,=\,III's cosmological hidden geometry
$\Omega_7/(4 \Omega_{217}) \approx 2.5 \times 10^{120}$ (the
cascade's native horizon-entropy at the cosmic scale, with the
$1/4$ factor derived from boundary dominance at $d = 4$, not
imported from semiclassical Bekenstein--Hawking). The paper develops
this \emph{three-way cascade match} as a saturation reading:
$\Lambda$ stays fixed at the Part~0 dimensional value while the
prime-cascade $\Sigma(N)$ and the dimensional-cascade hidden
geometry agree at the cosmic horizon scale near the current cosmic
depth. A dynamical companion result exhibits a Hawking-radiation
analog --- spacetime's share of total cascade content decays as
$1/N$ per Planck tick, matching the de~Sitter Hubble rate within a
$\sqrt{3}$ factor (the cascade's own Friedmann-projection
prefactor). The paper does not derive the distinguished dimensions,
the spacetime uniqueness at $d = 4$, or the cascade's $\Lambda$
prediction generatively from prime structure alone; those remain
Parts 0 and~III respectively. The cascade's foundational footprint
stays at pure $\mathrm{RCA}_0$ inherited from the Prelude.}}
\end{center}

\begin{abstract}
The Prelude forces $\pi$ as a convergence target from the Euler
product on primes and establishes the cascade's dimension tower over
$\mathbb{N}$. Part~0 uses Gamma-function structure on integer
arguments to derive the four distinguished dimensions $\{5, 7, 19,
217\}$ and the cascade invariant $\sim 10^{-120}$. This paper bridges
those two derivations by reading the cascade tick-by-tick in
prime-cascade terms: each Planck tick adds an integer that is either
\emph{atomic} (a prime, a fresh orthogonal axis with no relationships
to prior structure) or \emph{derived} (a composite, a lattice point
in the divisibility graph with back-references to its divisors).
Spacetime $(d \in \{1, 2, 3, 4\})$ is the cascade's
\emph{primordial era} --- the first four ticks --- with $d = 4$ as
the cascade's first derived event. After the primordial era, every
later integer carries back-references to a subset of $\{1, 2, 3, 4\}$,
and these back-references propagate recursively through the
divisibility lattice. The natural divisor-weighted content invariant
of the cascade at depth $N$ is $\Sigma(N) = \sum_{n \leq N} \sigma(n)
\sim (\pi^2/12) N^2$, with the prefactor $\pi^2/12 = \zeta(2)/2$
inheriting $\pi$ from the same Euler product the Prelude commits to.
At the current cosmic depth $N \sim 10^{60}$, $\Sigma(N) \approx
0.82 \times 10^{120}$. Paper~II\,=\,III\,\cite{part23} derives the
cascade-native horizon entropy $S = A/d$ from boundary dominance,
with the Bekenstein--Hawking factor $1/4$ at $d = 4$ identified as
the cascade's boundary-to-volume ratio at the observer's dimension
(not imported from semiclassical gravity), and applies it at the
cosmic horizon to give a hidden geometry
$\Omega_7/(4\Omega_{217}) \approx 2.5 \times 10^{120}$. Part~0's
dimensional cascade invariant inverse $\Lambda^{-1} \approx 10^{120}$
matches both. The three quantities --- prime-cascade $\Sigma(N)$,
dimensional $\Lambda^{-1}$, hidden-geometry
$\Omega_7/(4\Omega_{217})$ --- agree at order $10^{120}$ within
order-unity $\pi$-rational prefactors. The match is between cascade-
internal quantities, not between cascade and imported cosmological
results. Dynamically, the paper exhibits a Hawking-radiation analog:
spacetime's share of total cascade content,
$f_{\mathrm{spacetime}}(N) = 48/(\pi^2 N)$, decays as $1/N$ per
Planck tick with e-folding time exactly $N$ ticks, matching the
de~Sitter Hubble rate within a factor of $\sqrt{3}$ (the
cascade-derived Friedmann-projection prefactor). Information lost
from spacetime migrates into the cascade's higher-prime sector
rather than being destroyed; total cascade content is conserved.
The paper develops this as a \emph{saturation reading}: the
cascade's prime-cascade and dimensional-cascade routes converge on
the same horizon-entropy scale at the current cosmic depth, with
$\Lambda$ staying fixed at the Part~0 value. Whether the saturation
epoch is anthropically selected, structurally forced, or coincidental
remains open. The cascade's foundational footprint stays at pure
$\mathrm{RCA}_0$.
\end{abstract}

% ============================================================
\section{The Problem This Paper Addresses}\label{sec:problem}
% ============================================================

The Prelude derives the cascade series' starting structure --- the
dimension tower
$\{(\mathbb{R}^d, B^d)\}_{d \in \mathbb{N}}$ of finite-dimensional
Euclidean spaces with their unit balls --- from the single
pre-mathematical input $0 \neq 1$. The number-theoretic content is
forced by the Euler product on primes: $\pi$ enters the cascade as
the convergence target of $\sqrt{6\,\zeta_N(2)}$, where $\zeta_N(2)$
is the partial-computation form of $\sum 1/n^2$. The geometric
content (vector spaces, inner products, orthogonality at $\pi/2$) is
recovered from the prime-forced $\pi$ via Hurwitz's theorem and
elementary linear algebra.

Part~0 picks up from this starting structure and derives the cascade's
\emph{distinguished dimensions} $\{5, 7, 19, 217\}$ from Gamma-function
critical points on the integer axis, together with the cascade
invariant $\sim 10^{-120}$ as a product $\Omega_{19} \cdot \Omega_{217}$
of sphere areas at two of those dimensions, modified by a Gram
correction.

Between these two derivations a structural question remains open:
\emph{how does the prime structure of the Prelude express itself
across dimensions, before the Gamma-function machinery of Part~0
takes over?} The Prelude gives $\pi$; Part~0 evaluates $\Omega_d$ at
specific integer $d$. The link is mediated only through the constant
$\pi$ entering $\Omega_d = 2\pi^{d/2}/\Gamma(d/2)$. The full
arithmetic structure of $\mathbb{N}$ --- divisibility, the prime
factorisations of intermediate integers, the recursive back-reference
graph that the cascade builds tick by tick --- plays no explicit role
in this mediation.

This paper makes that arithmetic structure explicit. It reads the
cascade's tick-by-tick activity through the lens of the prime
cascade, identifies the structural events (atomic ticks, derived
ticks), exhibits the back-reference accumulation that fills the
cascade's foundational dimensions with content as cosmic time
advances, and surfaces a natural divisor-weighted quadratic invariant
that reproduces the order of magnitude of the cascade's invariant
scale at the current cosmic depth. The bridge is structural, not
generative: the distinguished dimensions and the cascade invariant
remain Part~0 content; what changes is the reading of \emph{how} the
prime structure is expressed by the time we reach Part~0.

% ============================================================
\section{From the Prelude}\label{sec:from-the-prelude}
% ============================================================

The relevant Prelude results, used here as background:

\begin{itemize}
\item Definition: $0 \neq 1$ as sole pre-mathematical input
(\xref{prelude}{def:axiom}).
\item Principle: austerity (parameter economy, minimal strength,
unobservable quotient) as the operational content of sole-input
status (\xref{prelude}{princ:austerity}).
\item Theorem: the cardinal index $d$ ranges over $\mathbb{N}$
(\xref{prelude}{thm:iterate}).
\item Theorem: the integer structure of $\mathbb{N}$ forces $\pi$ as
a convergence target via the Euler product on primes
(\xref{prelude}{thm:pi-from-primes}).
\item Theorem: the $d$ cumulated states are pairwise orthogonal in
$\mathbb{R}^d$ with $\langle e_i, e_j \rangle = \delta_{ij}$
(\xref{prelude}{thm:orth}).
\end{itemize}

\noindent The cascade's foundational footprint is pure
$\mathrm{RCA}_0$: natural numbers, computable functions on them, and
computable reals. Every analytic-looking quantity is a sequence of
rational approximations with $\mathrm{RCA}_0$-expressible convergence
behaviour. The Monotone Convergence Theorem and stronger analytic
principles serve as bookkeeping shorthand in downstream papers, not
as foundational commitments (see the Prelude status box). This paper
inherits that footprint without modification.

What the Prelude does \emph{not} establish, and what this paper
takes up:

\begin{itemize}
\item Tick-by-tick activity of the cascade beyond the existence
of the tower.
\item Structural distinction between prime ticks and composite ticks.
\item The back-reference graph that the cascade builds in the
divisibility lattice.
\item How spacetime dimensions $\{1, 2, 3, 4\}$ accumulate content
from later integer ticks.
\item A natural quadratic-in-$N$ structural invariant of the cascade
at resolution depth $N$.
\end{itemize}

% ============================================================
\section{The Prime Cascade}\label{sec:prime-cascade}
% ============================================================

Read tick by tick, the cascade adds one integer at each Planck tick:
$1, 2, 3, 4, 5, \ldots$ Each tick adds a node to the cascade's
structural record. Two kinds of node arise.

\begin{definition}[Atomic and derived ticks]\label{def:atomic-derived}
At tick $n$ the cascade adds the integer $n$ to its structure. The
tick is \emph{atomic} if $n$ is prime and \emph{derived} if $n$ is
composite (i.e., $n \geq 4$ with a non-trivial divisor). The unit
$n = 1$ is the trivial root; it is neither atomic nor derived.
\end{definition}

The structural content of a tick is determined by the divisor set
$\{d : d \mid n\}$ of the entering integer.

\begin{observation}[Atomicity is intrinsic]\label{obs:atomic-intrinsic}
Tick $n$ is atomic if and only if the only divisors of $n$ are $1$
and $n$ itself. Equivalently: atomic ticks are exactly those for
which the entering integer has no non-trivial relationship to any
prior tick. Compositeness corresponds to having at least one
non-trivial back-reference. The criterion is local to the entering
integer and requires no comparison with the rest of the cascade.
\end{observation}

\begin{definition}[Super primes, taken primes]\label{def:super-taken}
At resolution depth $N$, an atomic tick $p \leq N$ is \emph{super}
(or \emph{untaken}) if $2p > N$, and \emph{taken} if $2p \leq N$.
Equivalently: a prime $p$ is super at $N$ iff no composite multiple
of $p$ has yet entered the cascade. It is taken iff at least one
composite multiple ($2p$ being the smallest) has entered.
\end{definition}

\noindent The set of super primes at depth $N$ is the cascade's
\emph{leading edge}: orthogonal axes with no relationships in either
direction (no non-trivial divisors below; no composite multiples
above). Bertrand's postulate guarantees the leading edge is
non-empty at every $N \geq 2$: there is always at least one super
prime.

\begin{theorem}[Atomic-rate decay]\label{thm:atomic-rate}
The asymptotic density of atomic ticks at depth $N$ is $1/\ln N$
(Prime Number Theorem). The cumulative atomic fraction
$\pi(N)/N \to 0$ as $N \to \infty$.
\end{theorem}

\begin{proof}
This is the Prime Number Theorem in its standard form, provable in
$\mathrm{RCA}_0 + \mathrm{MCT}$ but with the relevant
$\mathrm{RCA}_0$-expressible content being: $\pi(N)/N$ is a
computable sequence whose convergence to zero is Cauchy in the form
$\forall\varepsilon\,\exists N$ such that $\pi(N)/N < \varepsilon$.
\end{proof}

\noindent The atomic-rate decay tells us the cascade was
atomic-dominated only briefly. The crossover from
atomic-majority to derived-majority happens at $N = 9$: at $N = 9$
the cascade has $\pi(9) = 4$ atomic ticks (primes $2, 3, 5, 7$) and
$9 - 4 - 1 = 4$ derived ticks (composites $4, 6, 8, 9$), and at
$N = 10$ derived ticks permanently outnumber atomic ones. By
$N = 100$ the cumulative atomic fraction is $25\%$; by $N = 10^6$
it is below $8\%$; at the current cosmic depth
$N \sim 10^{60}$ it is approximately $1/\ln(10^{60}) \approx 0.7\%$.

\begin{remark}[The atomic-dominated era is unbelievably brief]
The cascade spent its first nine Planck ticks in the atomic-dominated
regime --- approximately $10^{-43}$ seconds after the resolution
began. From tick~10 onward (still within $10^{-43}$ seconds) the
cascade has been derived-dominated. The remaining $\sim 10^{60}$
ticks of cosmic history correspond to derived-dominated lattice
filling at a rate that has decayed to less than one atomic event per
hundred ticks.
\end{remark}

% ============================================================
\section{The Primordial Era (Ticks 1--4)}\label{sec:primordial-era}
% ============================================================

The cascade's foundational dimensions are laid down in the first four
ticks. Each carries a specific structural role.

\begin{itemize}
\item \textbf{Tick 1: the unit.} $n = 1$ is the trivial root of the
divisibility lattice. Every later integer back-references it. The
unit is neither atomic nor derived in the sense of
Definition~\ref{def:atomic-derived}; it is the foundational element.
\item \textbf{Tick 2: first prime.} $n = 2$ is the first atomic
event. It introduces the cascade's first orthogonal axis, the only
prime that is also the additive successor of the unit.
\item \textbf{Tick 3: second prime.} $n = 3$ is the second atomic
event. Together with tick~2 it establishes the first non-trivial
arrangement of orthogonal axes.
\item \textbf{Tick 4: first derived event.} $n = 4 = 2^2$ is the
cascade's first composite. It is the first integer with a
non-trivial back-reference structure: it points to tick~2 with
weight $2$ (the only prime factor) and to tick~1 trivially. The
cascade's first lattice point.
\end{itemize}

\noindent A structural observation:

\begin{observation}[$d = 4$ as the cascade's first derived event]\label{obs:d4-first-derived}
The integer $4 = 2^2$ is the smallest composite. Its divisor lattice
is $\{1, 2, 4\}$, and it sits as the first node of the cascade's
divisibility graph above the atomic primes $\{2, 3\}$. Within the
primordial era, $d = 4$ is the unique non-atomic dimension.
\end{observation}

The observation has two immediate consequences:

\begin{itemize}
\item $d = 4$ is the cascade's first ``filling event'' --- the first
time the cascade adds a node whose role is to populate the lattice
of existing prime axes rather than to introduce a new axis.
\item $d = 4$ is the last dimension of the cascade's primordial era
before the first distinguished dimension $d = 5$ of Part~0 enters.
The dimensions $\{1, 2, 3, 4\}$ form the cascade's foundational
era, in which no specific dimension has yet been singled out by
Gamma-function critical-point machinery.
\end{itemize}

\begin{remark}[Spacetime as the primordial era]
The dimensions $\{1, 2, 3, 4\}$ are the cascade's foundational era
in the prime-cascade reading. They are also --- by the formal
derivation of \xref{part3}{paperdoc} (Lovelock uniqueness intersected
with Clifford periodicity) --- the dimensions of observable
spacetime. The two characterisations agree: spacetime is the
foundational era of the cascade. The prime-cascade observation does
not replace the Part~III derivation; it provides a structural
reading consistent with it. The observer lives at the boundary of the
primordial era, in the sense made formal by the Part~III argument.
\end{remark}

% ============================================================
\section{How Spacetime Fills with Content}\label{sec:spacetime-fills}
% ============================================================

After the primordial era ends at tick~4, every later integer
$n \geq 5$ carries back-references to a subset of $\{1, 2, 3, 4\}$
determined by its divisor structure. The back-references are
recursive: each one reaches a node that itself receives further
back-references from future ticks. The cascade does not lay down the
foundational dimensions and then leave them; it continually deposits
content into them via the back-reference graph.

\subsection*{Direct back-references}

For each $d \in \{2, 3, 4\}$, the integers $n > d$ with $d \mid n$
form an arithmetic progression of density $1/d$:
\begin{itemize}
\item Density of direct back-references to $d = 2$: $1/2$
(every even integer).
\item Density of direct back-references to $d = 3$: $1/3$
(every multiple of $3$).
\item Density of direct back-references to $d = 4$: $1/4$
(every multiple of $4$).
\end{itemize}

\noindent Per tick, the inclusion-exclusion total contribution to
spacetime $\{1, 2, 3, 4\}$ is approximately one back-reference. The
foundational dimensions are not uniformly loaded: $d = 2$ accumulates
content at twice the rate of $d = 4$.

\begin{theorem}[Cumulative direct back-references]\label{thm:direct-backref}
At depth $N$, the cumulative direct back-reference count for
spacetime dimension $d \in \{2, 3, 4\}$ is $\lfloor N/d \rfloor$,
with $\mathrm{RCA}_0$-expressible convergence under the partial-
computation reading.
\end{theorem}

\noindent The counts are: $\sim N/2$ for $d = 2$, $\sim N/3$ for
$d = 3$, $\sim N/4$ for $d = 4$. The leading factor for the total
spacetime content from direct back-references is therefore
$N \cdot (1/2 + 1/3 + 1/4) - (\text{inclusion-exclusion}) \approx
0.83\, N$.

\subsection*{Recursive back-references}

The direct back-reference count under-states the cascade's content
deposition. Each back-reference $n \to d$ passes through intermediate
divisors of $n$, and each of those intermediate nodes itself receives
future references. Counting \emph{cover-edge paths} from $n$ to $d$
in the divisibility lattice, rather than direct edges, gives the
correct content measure.

\begin{theorem}[Cover-chain count]\label{thm:chain-count}
Let $n$ be a positive integer with $d \mid n$ and write
$n/d = p_1^{a_1} p_2^{a_2} \cdots p_r^{a_r}$ in prime factorisation.
The number of maximal cover-edge chains from $n$ to $d$ in the
divisibility lattice $(\mathbb{N}, \mid)$ is the multinomial
coefficient
\[
\mathrm{chains}(n, d) \;=\; \frac{(a_1 + a_2 + \cdots + a_r)!}{a_1!\, a_2! \cdots a_r!}.
\]
\end{theorem}

\begin{proof}
A maximal chain from $n$ down to $d$ corresponds to a sequence of
prime-drops removing one factor of some $p_i$ at each step. The
total number of drops is $\Omega(n/d) = a_1 + \cdots + a_r$, and at
each step we must choose which prime to drop. The number of such
sequences, modulo equivalent reorderings of identical prime-drops,
is the multinomial coefficient stated.
\end{proof}

\noindent Worked examples:
\begin{itemize}
\item $n = 12, d = 4$: $n/d = 3$, chains $= 1!/1! = 1$. Single chain
$12 \to 4$.
\item $n = 24, d = 4$: $n/d = 6 = 2 \cdot 3$, chains $= 2!/1!1! = 2$.
Chains $24 \to 12 \to 4$ and $24 \to 8 \to 4$.
\item $n = 48, d = 4$: $n/d = 12 = 2^2 \cdot 3$, chains $= 3!/2!1!
= 3$.
\item $n = 30030 = 2 \cdot 3 \cdot 5 \cdot 7 \cdot 11 \cdot 13,\,
d = 1$: chains $= 6! = 720$. Primorials contribute factorially many
chains.
\end{itemize}

\subsection*{Asymptotic cumulative content}

\begin{theorem}[Cumulative chain content]\label{thm:chain-cumulative}
The cumulative chain-count content of spacetime dimension $d$ at
depth $N$ is
\[
T_d(N) \;=\; \sum_{\substack{m \leq N \\ d \mid m}} \mathrm{chains}(m, d) \;=\; \sum_{k \leq N/d} \mathrm{chains}(k, 1).
\]
For typical $k$ near $N$, $\mathrm{chains}(k, 1)$ has average value
asymptotically $(\ln\ln N)!\, /\, (\text{factorial corrections})$
(Erdős--Kac). The dominant contribution to $T_d(N)$ comes from
integers with many distinct prime factors --- in particular from
primorial-like integers, whose chain count grows factorially in the
number of prime factors.
\end{theorem}

\noindent The chain-count rate per tick is therefore not constant
but grows with $N$: at any cosmic epoch, the cascade is depositing
content into spacetime at a rate that exceeds the rate at any
earlier epoch. The growth is sub-exponential --- bounded by
$(\ln\ln N)!$ for typical contributions --- but unbounded as
$N \to \infty$.

\begin{remark}[Acceleration without exponentiation]
The cascade's content deposition into spacetime is not constant in
time; it accelerates. But the acceleration is slow: at $N = 10^6$ the
typical chain count per tick is approximately $7$; at $N = 10^{60}$
it is approximately $120$; at $N = 10^{1000}$ it would reach
approximately $5000$. The cascade is genuinely producing more
content per tick as cosmic time advances, but the growth is in the
$(\ln \ln N)!$ regime, far slower than de~Sitter exponential
expansion. The acceleration in content deposition is therefore a
distinct phenomenon from cosmic acceleration; this paper does not
identify them.
\end{remark}

% ============================================================
\section{The Natural Quadratic Invariant}\label{sec:quadratic-invariant}
% ============================================================

The chain count of Section~\ref{sec:spacetime-fills} counts paths
with equal weight, regardless of which divisor a back-reference
terminates at. A finer measurement uses the divisor's \emph{value}
as a weight: a back-reference to divisor $d$ contributes $d$ to the
count. This gives the cascade's \emph{divisor-weighted content}.

\begin{definition}[Divisor-weighted content]\label{def:divisor-weighted}
The divisor-weighted content of the cascade at depth $N$ is
\[
\Sigma(N) \;=\; \sum_{n \leq N} \sigma(n) \;=\; \sum_{n \leq N} \sum_{d \mid n} d.
\]
\end{definition}

\noindent Each back-reference $n \to d$ contributes weight $d$ to
$\Sigma(N)$. Small divisors are referenced often but each reference
is light-weight; large divisors are referenced rarely but each
reference is heavyweight. The two effects combine to give a
quadratic asymptotic.

\begin{theorem}[Asymptotic of divisor-weighted content]\label{thm:sigma-asymptotic}
For large $N$,
\[
\Sigma(N) \;\sim\; \frac{\pi^2}{12} N^2 \;=\; \frac{\zeta(2)}{2} N^2.
\]
\end{theorem}

\begin{proof}
Exchange the order of summation:
\[
\Sigma(N) \;=\; \sum_{d \leq N} d \cdot \lfloor N/d \rfloor.
\]
For each $d$, the contribution is $d \cdot (N/d - \{N/d\}) =
N - d\{N/d\}$. Summing over $d \leq N$ gives $N^2 - O(N \log N)$ in
naive form; the precise prefactor $\pi^2/12$ arises from the
$\zeta(2) = \pi^2/6$ identity applied via Dirichlet's method on the
average order of $\sigma(n)$, namely $\sigma(n)/n \to \pi^2/6$ on
average. Standard derivation; see any analytic number theory text.
The Cauchy form of the convergence is
$\mathrm{RCA}_0$-expressible.
\end{proof}

\noindent The prefactor $\pi^2/12 = \zeta(2)/2$ is the Euler product
on primes from the Prelude, divided by $2$. The cascade's quadratic
invariant inherits $\pi$ from exactly the same prime structure that
$\pi$ inherits from in \xref{prelude}{thm:pi-from-primes}.

\begin{observation}[Quadratic content scale]\label{obs:quadratic-scale}
At any cosmic depth $N$, the cascade's divisor-weighted content
$\Sigma(N)$ scales as $(\pi^2/12) N^2$ --- a single natural quantity
that depends on the cascade's resolution depth and the prime-forced
constant $\pi^2$, with no other inputs. At $N = 10^{60}$,
$\Sigma(N) \approx 8.2 \times 10^{119}$.
\end{observation}

% ============================================================
\section{Bridge to Part 0: Numerical Convergence at Cosmic Epoch}\label{sec:bridge-part0}
% ============================================================

Part~0 derives the cascade invariant via the dimensional route. The
relevant chain of results (\xref{part0}{thm:tower} and
\xref{part0}{sec:the-cascade-invariant}):
\begin{itemize}
\item Four distinguished dimensions $\{5, 7, 19, 217\}$ emerge as
Gamma-function critical points on the integer axis.
\item Sphere areas $\Omega_{d-1} = 2\pi^{d/2}/\Gamma(d/2)$ at these
dimensions take specific values.
\item The cascade invariant is the product
$(18/\pi^3) \cdot \Omega_{19} \cdot \Omega_{217} \cdot e^{\mathrm{Gram}}$,
which evaluates to approximately $10^{-120}$ in reduced Planck units.
\end{itemize}

\noindent The numerical match between the two routes at the current
cosmic depth:

\begin{center}
\begin{tabular}{lll}
\toprule
Route & Quantity & Value at $N \sim 10^{60}$ \\
\midrule
Prime cascade ($\Sigma^{-1}$) & $12/(\pi^2 N^2)$ & $\sim 1.2 \times 10^{-120}$ \\
Dimensional cascade & $(18/\pi^3) \Omega_{19} \Omega_{217} e^{\mathrm{Gram}}$ & $\sim 10^{-120}$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent Both routes give approximately $10^{-120}$ within
order-unity factors. The prefactors $\pi^2/12$ in the prime-cascade
route and $18/\pi^3$ in the dimensional route involve different
powers of $\pi$; the match in magnitude is non-trivial.

Three readings of this convergence are available:

\begin{itemize}
\item[(a)] \emph{Same underlying quantity.} $\Sigma(N)^{-1}$ and the
dimensional cascade invariant are two expressions of the same
cascade content, prime-structural and geometric. If true, there
should be an identity at the cascade's preferred $N$ relating
$12/(\pi^2 N^2)$ to $(18/\pi^3) \Omega_{19} \Omega_{217}
e^{\mathrm{Gram}}$; the cascade's distinguished dimensions and
the cascade's resolution depth would be locked together by this
identity.
\item[(b)] \emph{Numerically coincident but distinct.} $\Sigma(N)$
depends on $N$; the dimensional cascade invariant does not. They
agree at the current cosmic epoch but diverge at other epochs. The
match would then be a property of the current $N$, possibly
anthropic.
\item[(c)] \emph{Projections of a deeper cascade structure.} Both
quantities are valid expressions of a single underlying cascade
invariant, the prime cascade giving the arithmetic form and the
dimensional cascade the geometric form, with their match at the
current cosmic epoch forced by the cascade's resolution depth being
in its mature regime.
\end{itemize}

\noindent Reading~(a) is observationally disfavoured: identifying
$\Lambda(N) = 1/\Sigma(N)$ literally gives a time-varying $\Lambda
\propto 1/N^2$, which produces power-law cosmology with scale factor
$a(t) \propto t^{2/\pi}$ (decelerating expansion), inconsistent with
the observed late-time cosmic acceleration. Reading~(b) treats the
match as a coincidence at the current $N$. Reading~(c) is developed
explicitly in Section~\ref{sec:holographic-match} below: the
cascade's hidden-geometry formula $S = A/d$, derived in
Paper~II\,=\,III\,\cite{part23} from boundary dominance at the
observer's dimension $d = 4$, provides a \emph{third} cascade-
internal route to the $10^{120}$ scale via the cosmic-horizon
hidden geometry $\Omega_7/(4\Omega_{217})$. Three cascade-native
quantities (prime-cascade $\Sigma(N)$, dimensional $\Lambda^{-1}$,
hidden-geometry $\Omega_7/(4\Omega_{217})$) agree at order $10^{120}$
at the current cosmic depth, with explicit $\pi$-rational
prefactor relations. $\Lambda$ stays fixed at the Part~0 value;
cosmic expansion stays de~Sitter; the three routes converge on the
same horizon-entropy scale.
Section~\ref{sec:hawking-analog} then exhibits the dynamical
companion: spacetime's share of total cascade content decays as
$1/N$ per Planck tick (Hawking-radiation-like, with information
migrating to the higher-prime sector rather than being destroyed),
matching the de~Sitter Hubble rate within a $\sqrt{3}$ factor
traceable to Friedmann's $H^2 = \Lambda/3$.

% ============================================================
\section{Informational Density and the Three-Way Cascade Match}\label{sec:holographic-match}
% ============================================================

The numerical convergence of Section~\ref{sec:bridge-part0} can be
made sharper by reading the cascade's divisor-weighted content as
\emph{informational density} and comparing it to the cascade's
native horizon-entropy formula derived in Paper~II\,=\,III. The
result is a \emph{three-way agreement} between cascade-internal
quantities --- Part~0's dimensional invariant $\Lambda^{-1}$,
Paper~II\,=\,III's hidden geometry $\Omega_7/(4\Omega_{217})$, and
this paper's prime-cascade quadratic invariant $\Sigma(N)$ --- all
of order $10^{120}$ at the current cosmic depth, with explicit
$\pi$-rational prefactors. The Bekenstein--Hawking factor of $1/4$
appearing in standard black-hole thermodynamics is itself cascade-
derived (Paper~II\,=\,III, Theorem~\xref{part23}{thm:bh}) as the
boundary-to-volume ratio $1/d$ at the observer's dimension
$d = 4$; nothing about the horizon entropy is imported from
semiclassical gravity.

\subsection*{Cascade content as informational density}

The divisor-weighted content $\Sigma(N) = \sum_{n \leq N} \sigma(n)$
counts every back-reference in the cascade's divisibility lattice up
to depth $N$, weighted by the divisor's value. In information-
theoretic terms it measures the cascade's accumulated structural
content, with each lattice point contributing in proportion to the
content of its divisor. By Theorem~\ref{thm:sigma-asymptotic} the
asymptotic is
\[
\Sigma(N) \;\sim\; \frac{\pi^2}{12}\, N^2.
\]
At the current cosmic depth $N \sim 10^{60}$ Planck ticks,
\[
\Sigma(10^{60}) \;\approx\; 0.822 \times 10^{120} \;\approx\; 8.2 \times 10^{119}
\]
in cascade-natural units.

\subsection*{The cascade-native horizon entropy}

Paper~II\,=\,III\,\cite{part23}, Theorem~\xref{part23}{thm:bh}
(``Hidden geometry behind a horizon''), establishes the cascade's
native horizon-entropy formula
\[
S \;=\; \frac{A}{d}
\]
for a horizon of area $A$ measured in cascade units, with the factor
$1/d$ derived from \emph{boundary dominance}
(Paper~0, Theorem~\xref{part0}{thm:boundary}): $\Omega_{d-1}/V_d = d$,
hence $V_d = \Omega_{d-1}/d$. The Bekenstein--Hawking factor $1/4$ at
$d = 4$ is the cascade's boundary-to-volume ratio at the observer's
dimension --- not an imported semiclassical result. Paper~II\,=\,III's
``Cosmological hidden geometry'' subsection applies this formula at
the cosmic horizon:
\[
A_{\mathrm{cosmic}} \;=\; \frac{\Omega_7}{\Omega_{217}}
\quad\text{(cascade hierarchy)},
\qquad
S_{\mathrm{cosmic}} \;=\; \frac{A_{\mathrm{cosmic}}}{4} \;=\; \frac{\Omega_7}{4\,\Omega_{217}}.
\]
Numerically: $\Omega_7 = 2\pi^4/\Gamma(4) = \pi^4/3 \approx 32.5$,
$\Omega_{217} \approx 10^{-118.8}$ (Stirling), so $\Omega_7/\Omega_{217}
\approx 10^{121}$ and $S_{\mathrm{cosmic}} \approx 2.5 \times 10^{120}$.

\subsection*{The dimensional cosmological constant}

Part~0, Theorem~\xref{part0}{thm:tower} and
\xref{part0}{sec:the-cascade-invariant}, derives
\[
\Lambda \;=\; \frac{18}{\pi^3}\, \Omega_{19}\, \Omega_{217}\, e^{\mathrm{Gram}} \;\approx\; 10^{-120}
\]
in reduced Planck units. Its inverse,
$\Lambda^{-1} \approx 10^{120}$, is the third cascade-internal
scale at the cosmic horizon. (Paper~II\,=\,III's ``Cosmological
hidden geometry'' identifies $\Lambda^{-1}$ with $S_{\mathrm{cosmic}}$
up to cascade prefactors: $\Lambda \sim 10^{-120}$ is the inverse of
the hidden geometry, seen from the dimensional side.)

\subsection*{The three-way match}

\begin{observation}[Three cascade-native scales at the cosmic horizon]\label{obs:three-way}
At the current cosmic depth $N \sim 10^{60}$, three quantities
derived independently from the cascade's structure take values of
order $10^{120}$, with explicit prefactors:
\[
\begin{aligned}
\Sigma(N) &\;=\; \tfrac{\pi^2}{12}\, N^2
&&\approx\; 0.822 \times 10^{120}
&&\text{(prime cascade, this paper)} \\
S_{\mathrm{cosmic}} &\;=\; \tfrac{\Omega_7}{4\,\Omega_{217}}
&&\approx\; 2.5 \times 10^{120}
&&\text{(hidden geometry, \xref{part23}{thm:bh})} \\
\Lambda^{-1} &\;=\; \left(\tfrac{18}{\pi^3} \Omega_{19} \Omega_{217} e^{\mathrm{Gram}}\right)^{-1}
&&\approx\; 10^{120}
&&\text{(dimensional invariant, \xref{part0}{thm:tower})}
\end{aligned}
\]
All three are cascade-internal: $\Sigma(N)$ from the Euler product
and divisor sum on $\mathbb{N}$; $S_{\mathrm{cosmic}}$ from boundary
dominance and the sphere-area hierarchy; $\Lambda^{-1}$ from the
distinguished-dimension product. Their order-of-magnitude agreement
at $\sim 10^{120}$ is unambiguous; the precise prefactors differ
within order-unity $\pi$-rational factors.
\end{observation}

\subsection*{Prefactor relations}

Stating the relations explicitly:

\textbf{$S_{\mathrm{cosmic}}$ vs.\ $\Sigma(N)$.} The ratio is
\[
\frac{S_{\mathrm{cosmic}}}{\Sigma(N)}
\;=\; \frac{\Omega_7/(4\,\Omega_{217})}{(\pi^2/12)\, N^2}
\;=\; \frac{3\,\Omega_7}{\pi^2\,\Omega_{217}\,N^2}.
\]
For $\Omega_7/\Omega_{217} \approx 10^{121}$ and $N^2 \approx
10^{120}$, this ratio is approximately $3 \cdot 10^{121}/(\pi^2 \cdot
10^{120}) \approx 30/\pi^2 \approx 3$. So $S_{\mathrm{cosmic}}$
exceeds $\Sigma(N)$ by an order-unity factor of about $3$ at the
current cosmic depth. Equivalently, the prime-cascade saturation
epoch (the $N$ at which $\Sigma(N) = S_{\mathrm{cosmic}}$) is at
$N_{\mathrm{sat}} = \sqrt{3\,\Omega_7/(\pi^2\,\Omega_{217})}
\approx 1.7 \times 10^{60}$, within a factor of $2$ of the
current cosmic depth.

\textbf{$\Lambda^{-1}$ vs.\ $S_{\mathrm{cosmic}}$.} Paper~II\,=\,III
(``Cosmological hidden geometry'') identifies these directly: $\Lambda$
is the inverse of the hidden geometry. The relation is
\[
\Lambda \cdot S_{\mathrm{cosmic}} \;\sim\; 1,
\]
with prefactor differences absorbed in the Gram correction
$e^{\mathrm{Gram}}$ of the Part~0 formula. Both quantities are
$\sim 10^{120}$ in inverse-Planck units; the precise identity
involves cascade prefactors that match within order-unity
$\pi$-rational corrections.

\textbf{$\Lambda \cdot N^2$ at the saturation epoch.} Setting
$\Sigma(N) = S_{\mathrm{cosmic}}$ defines $N_{\mathrm{sat}}$ as
above. With $\Lambda \approx 10^{-120}$ and $N_{\mathrm{sat}}
\approx 1.7 \times 10^{60}$,
$\Lambda \cdot N_{\mathrm{sat}}^2 \approx 3$, a cascade-natural
$\pi$-rational order-unity quantity. The Dirac coincidence
$\Lambda \cdot N^2 \sim 1$ is the cascade's statement that the
universe's age in Planck ticks lands within an order-unity factor
of the cascade's saturation point.

\subsection*{Rate considerations}

The match in magnitudes can be supplemented by a comparison of
rates. The cascade's content-generation rate, differentiating
$\Sigma(N) = (\pi^2/12) N^2$, is
\[
\frac{d\Sigma}{dt} \;=\; \frac{\pi^2}{6}\, N \quad\text{per Planck tick.}
\]
At $N \sim 10^{60}$ this is $\sim 1.6 \times 10^{60}$ content-units
per tick. The corresponding cosmological rate is the Hubble-time-
weighted horizon entropy $H \cdot S_{\mathrm{cosmic}}$, with
$H = \sqrt{\Lambda/3} \sim 1/N$ in Planck units; this gives
$H \cdot S_{\mathrm{cosmic}} \sim S_{\mathrm{cosmic}}/N \sim
N$. Both rates scale as $N$ at the current epoch.

This rate comparison is more speculative than the magnitude match.
The cascade's $\Sigma(N)$ rate is cascade-internal; the cosmological
$H \cdot S_{\mathrm{cosmic}}$ rate uses the Hubble rate of standard
cosmology (which the cascade derives via Lovelock + Friedmann
downstream in Part~III). Whether the rate balance is forced or
incidental is open.

\subsection*{The saturation interpretation}

The three-way magnitude match supports a reading in which:
\begin{itemize}
\item The cascade's prime-structural content $\Sigma(N)$ matches the
cascade's geometric hidden-geometry $S_{\mathrm{cosmic}}$ at
$N \approx N_{\mathrm{sat}} \approx 10^{60}$.
\item The dimensional cascade invariant $\Lambda^{-1}$ matches both
at the same order, with prefactor differences absorbed by Gram
corrections and cascade-natural $\pi$-rational factors.
\item The cosmological constant $\Lambda \approx 10^{-120}$ is the
inverse of the cascade's hidden geometry at the cosmic horizon
(Paper~II\,=\,III).
\item The cascade's preferred resolution depth (the universe's age
in Planck ticks) sits at the saturation point where these three
cascade-internal scales agree.
\end{itemize}

Under this reading the saturation point is the cascade's structural
``now'': the cosmic depth at which the cascade's accumulated content
in the prime-cascade reading matches the cascade's hidden geometry
in the dimensional reading. The two readings of the cascade --- prime
cascade in this paper, dimensional cascade in Part~0 and
Paper~II\,=\,III --- agree at this depth within order-unity factors.

\subsection*{$\Lambda$ stays constant}

Crucially, the saturation reading is consistent with constant
$\Lambda$ at the Part~0 value $\sim 10^{-120}$:
\begin{itemize}
\item $\Lambda$ is set by $\Omega_{19} \Omega_{217}$
(\xref{part0}{thm:tower}); structural, dimensional, fixed.
\item De~Sitter expansion at $H = \sqrt{\Lambda/3}$ is unchanged.
\item $S_{\mathrm{cosmic}} = \Omega_7/(4\Omega_{217})$ is a fixed
cascade ratio, independent of cosmic time.
\item Cascade content $\Sigma(N)$ grows quadratically with $N$. As
$N$ increases beyond $N_{\mathrm{sat}}$, $\Sigma$ exceeds
$S_{\mathrm{cosmic}}$; on a holographic-saturation reading, the
excess is shed across the cosmic horizon via expansion.
\end{itemize}

The Dirac coincidence $\Lambda \cdot N^2 \sim 1$ becomes the
statement \emph{the universe's age in Planck ticks is at the cascade's
saturation point, where the prime-cascade and dimensional-cascade
invariants agree on the horizon-entropy scale}. The cascade has run
for the right number of ticks to bring its two natural invariants
into agreement.

\subsection*{Comparison to the naive identity reading}

This is reading~(c) of Section~\ref{sec:bridge-part0} made specific
via Paper~II\,=\,III. The naive identity reading~(a),
$\Lambda(N) = 1/\Sigma(N)$ literally, takes the magnitude match as
a definitional identity and requires $\Lambda$ to vary as $1/N^2$,
giving power-law cosmology with scale factor $a(t) \propto t^{2/\pi}$
(decelerating). The saturation reading keeps $\Lambda$ fixed at the
Part~0 dimensional value and uses Paper~II\,=\,III's hidden geometry
to mediate between $\Sigma(N)$ and $\Lambda^{-1}$. Cosmic expansion
stays de~Sitter; the three-way match is between cascade-internal
quantities at the saturation epoch. Reading~(a) is observationally
falsified by cosmic acceleration; the saturation reading is
consistent with observed de~Sitter expansion.

\subsection*{What this reading does and does not assert}

It \emph{does} assert:
\begin{itemize}
\item Three cascade-internal quantities ($\Sigma(N)$,
$S_{\mathrm{cosmic}}$, $\Lambda^{-1}$) agree at order $10^{120}$ at
the current cosmic depth, with explicit $\pi$-rational prefactors
that differ by order-unity factors.
\item The cascade's horizon-entropy formula $S = A/d$ is derived in
Paper~II\,=\,III from boundary dominance; the Bekenstein--Hawking
factor $1/4$ at $d = 4$ is cascade-native, not imported from
semiclassical gravity.
\item The match is consistent with constant $\Lambda$, de~Sitter
expansion, and a cascade-internal account of the Dirac large-number
coincidence.
\end{itemize}

It \emph{does not} assert:
\begin{itemize}
\item That the prefactors in the three-way match coincide exactly.
$\Sigma(N)$ and $S_{\mathrm{cosmic}}$ differ by a factor of about
$3$ at the current cosmic depth; the saturation point
$N_{\mathrm{sat}}$ differs from the current $N$ by a factor of
about $2$. Both are within cascade-natural order-unity tolerances
but neither is forced from austerity alone.
\item That the rate balance $d\Sigma/dt \sim H \cdot
S_{\mathrm{cosmic}}$ is exact at all cosmic epochs. Under constant
$\Lambda$, the cascade's quadratic content growth is compatible with
the rate balance only locally near $N_{\mathrm{sat}}$.
\item That the saturation epoch is forced by austerity. Whether the
cascade's distinguished dimensions, its quadratic invariant, and
the cosmic depth at which all three agree are locked together by a
cascade-internal identity, or whether the agreement is anthropically
selected by observer existence, remains open.
\item Any modification of standard cosmology. The de~Sitter
expansion, $\Lambda$ value, and horizon-entropy formula are exactly
as derived in Part~0, Paper~II\,=\,III, and Part~III.
\end{itemize}

\subsection*{The asserted step of Paper~II\,=\,III, inherited}

Paper~II\,=\,III's Theorem~\xref{part23}{thm:bh} derives
$S = A/d$ from boundary dominance with one asserted step
(Paper~II\,=\,III, Remark~\xref{part23}{rem:sp23-status}): that the
content--area map is linear in $A$ with slope $1/d$ for any cascade
area, not only for $A = \Omega_{d-1}$ at the unit sphere. The cascade
verifies this empirically at $d = 3$ (BTZ) and $d = 4$ (Schwarzschild)
without parameter fitting. This paper inherits Paper~II\,=\,III's
asserted step: the three-way match relies on $S = A/d$ being valid
at the cosmic horizon area $A_{\mathrm{cosmic}} = \Omega_7/\Omega_{217}$,
not only at unit-sphere area. Closing the asserted step ---
a cascade-action derivation of the linear content--area
map --- would tighten the three-way match into a forced identity.

% ============================================================
\section{Spacetime as the Cascade's Hawking-Emitting Sector}\label{sec:hawking-analog}
% ============================================================

Section~\ref{sec:holographic-match} establishes a static three-way
match between cascade-internal quantities at the current cosmic
epoch. This section examines the cascade's \emph{dynamics}: how
spacetime's content evolves relative to total cascade content as
the prime tower climbs. The result is a Hawking-radiation analog
with cascade-derived rates that match de~Sitter cosmology in
functional form, with $\pi$- and $\sqrt{3}$-rational order-unity
prefactor differences traceable to the cascade-derived $\sqrt{3}$
of the Friedmann equation.

\subsection*{Spacetime's content, exactly}

The cascade's spacetime is the primordial era $d \in \{1, 2, 3, 4\}$
(Section~\ref{sec:primordial-era}). For each spacetime dimension
$d$, the integers $n \leq N$ with $d \mid n$ contribute weight $d$
each to the divisor-weighted content:
\[
C_{\mathrm{spacetime}}(N) \;=\; \sum_{d \in \{1, 2, 3, 4\}} d \cdot \lfloor N/d \rfloor.
\]
Each term contributes approximately $N$:
\[
C_{\mathrm{spacetime}}(N) \;\approx\; 4N.
\]
Spacetime's divisor-weighted content grows linearly with cosmic age,
gaining $4$ content-units per Planck tick. At $N \sim 10^{60}$,
$C_{\mathrm{spacetime}} \sim 4 \times 10^{60}$.

\subsection*{Spacetime's share of total cascade content}

Total cascade content $\Sigma(N) = (\pi^2/12) N^2$
(Theorem~\ref{thm:sigma-asymptotic}). Spacetime's share is therefore
\[
f_{\mathrm{spacetime}}(N) \;=\; \frac{C_{\mathrm{spacetime}}(N)}{\Sigma(N)}
\;=\; \frac{4N}{(\pi^2/12)\, N^2} \;=\; \frac{48}{\pi^2\, N}.
\]
At cosmic depth $N \sim 10^{60}$:
$f_{\mathrm{spacetime}} \approx 4.86 \times 10^{-60}$. Spacetime is
about one part in $2 \times 10^{59}$ of the cascade's total
accumulated content; the rest lives in higher-prime structural
content that doesn't have back-references to $\{1, 2, 3, 4\}$.

\subsection*{The share-decay rate}

\begin{theorem}[Spacetime-share decay]\label{thm:share-decay}
The spacetime share decays as
\[
\frac{1}{f_{\mathrm{spacetime}}} \frac{df_{\mathrm{spacetime}}}{dN}
\;=\; -\frac{1}{N}.
\]
The e-folding time for spacetime-share decay is exactly $N$ Planck
ticks --- equal to the cosmic age itself.
\end{theorem}

\begin{proof}
From $f = 48/(\pi^2 N)$, $df/dN = -48/(\pi^2 N^2)$. Dividing,
$(df/dN)/f = -1/N$. The e-folding time is
$|f/(df/dN)| = N$.
\end{proof}

\noindent The decay rate has no free parameters and no prefactor:
it is $1/N$ exactly, with $N$ the cosmic age in Planck ticks. Every
spacetime dimension is constantly losing share of the total
cascade content because the cascade's high-prime sector grows
quadratically while spacetime accumulates back-references only
linearly.

\subsection*{Comparison with de~Sitter Hubble dilution}

In de~Sitter cosmology with $\Lambda$ at the cascade's Part~0 value
and the cascade-natural identification $\Lambda \cdot N^2 \sim 1$:
\[
H \;=\; \sqrt{\Lambda/3} \;=\; \frac{1}{N\sqrt{3}}, \qquad
T_{\mathrm{Hubble}} \;=\; \frac{1}{H} \;=\; N\sqrt{3}.
\]
The Hubble e-folding time is $N\sqrt{3} \approx 1.73\, N$ Planck
ticks. The cascade's e-folding time is $N$ ticks exactly.

\begin{observation}[Share-decay matches Hubble rate within $\sqrt{3}$]
\label{obs:share-hubble}
The cascade's spacetime-share decay rate is $1/N$; the de~Sitter
Hubble rate is $1/(N\sqrt{3})$. The two have the same functional
form ($\propto 1/N$, decay-time linear in cosmic age) and differ
only by the prefactor $\sqrt{3}$ --- inherited from the Friedmann
equation $H^2 = \Lambda/3$, where the $1/3$ is the trace structure
of the four-dimensional Einstein tensor on a maximally-symmetric
vacuum.
\end{observation}

\subsection*{Total content generation versus horizon flux}

A second rate comparison: the cascade's total content-generation
rate against the de~Sitter horizon information flux.

\textbf{Cascade total-content generation rate.}
\[
\frac{d\Sigma}{dN} \;=\; \frac{\pi^2}{6}\, N \quad\text{per Planck tick.}
\]

\textbf{De~Sitter horizon information flux.} Using the standard
Bekenstein--Hawking $S_{\mathrm{dS}} = 3\pi/\Lambda$ (with the
factor $3$ from the de~Sitter horizon area
$A = 12\pi/\Lambda$, derivable cascade-natively at $d = 4$ via
Paper~II\,=\,III's $S = A/d$ if one uses $A = \Omega_7/\Omega_{217}$
in cascade units; here we use the GR-units version for comparison):
\[
H \cdot S_{\mathrm{dS}} \;=\; \frac{1}{N\sqrt{3}} \cdot 3\pi N^2
\;=\; \pi N \sqrt{3} \quad\text{per Planck tick.}
\]

\textbf{The ratio.}
\[
\frac{d\Sigma/dN}{H \cdot S_{\mathrm{dS}}}
\;=\; \frac{\pi^2/6}{\pi\sqrt{3}}
\;=\; \frac{\pi}{6\sqrt{3}}
\;\approx\; 0.302.
\]
The cascade's content-generation rate is about $30\%$ of the
de~Sitter horizon flux. Same functional form (linear in $N$),
prefactor differs by $\pi/(6\sqrt{3})$ --- again involving
$\sqrt{3}$ from Friedmann.

\subsection*{The Hawking-radiation analog}

The structural correspondence with Hawking radiation:

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{lll}
\toprule
Quantity & Hawking BH & Cascade spacetime \\
\midrule
``Object'' & Black hole, mass $M$ & Spacetime at cosmic depth $N$ \\
``Mass'' & $M$ (decreasing) & $C_{\mathrm{spacetime}} = 4N$ (increasing) \\
``Temperature'' & $T_{\mathrm{H}} = 1/(8\pi M)$ & Share-decay rate $1/N$ \\
Loss rate functional form & $dM/dt \sim -1/M^2$ & $df/dN \sim -1/N^2$ \\
Information destination & Outgoing thermal radiation & Higher-prime cascade sector \\
Information conservation & Resolved via correlations & Conserved (within cascade) \\
\bottomrule
\end{tabular}
\end{center}

\noindent The functional form $\propto -1/(\text{scale})^2$ matches
between Hawking $dM/dt$ and cascade $df/dN$. The cascade's
"object" gains absolute content over time while losing fractional
share, unlike a Hawking black hole that loses absolute mass and
eventually evaporates. This is the difference between a
finite-lifetime regime (BH) and a steady-growth regime (cascade).

\subsection*{Where the information goes}

The cascade content that doesn't accumulate in spacetime
accumulates in higher-prime structural content. Examples of
"high-prime" integers whose divisors don't include any of
$\{2, 3, 4\}$ beyond the trivial unit:
\begin{itemize}
\item $11 \cdot 13 = 143$. Divisors $\{1, 11, 13, 143\}$.
Back-reference to $\{1\}$ within spacetime; the rest is outside.
\item $11 \cdot 17 \cdot 19 = 3553$. Divisors include
$\{1, 11, 17, 19, 187, 209, 323, 3553\}$. Same.
\item Products of large primes generally: every new prime
$p_k \geq 5$ added to the cascade enables a new family of
composites that contribute to $\Sigma(N)$ without (much) contributing
to $C_{\mathrm{spacetime}}$.
\end{itemize}

Each new such composite added to the cascade contributes to total
$\Sigma$ without adding to spacetime's back-reference count. The
cascade's information increasingly accumulates in structurally
distant locations from the spacetime observer.

\subsection*{Information is conserved}

In contrast to Hawking's original picture (information apparently
lost behind the horizon, the paradox), the cascade conserves total
content: $\Sigma(N)$ is monotonically increasing. What decays is
the \emph{accessibility} of content to the spacetime observer, not
its existence within the cascade.

This structurally parallels modern resolutions of the Hawking
information paradox --- Page-curve evolution, fuzzball / firewall
proposals, replica wormholes --- in which information escapes the
black hole via correlations in the radiation, conserving total
information. The cascade's mechanism is more direct: information
migrates into structurally higher-prime cascade content, in
principle accessible but not via spacetime's natural
divisibility relations to $\{1, 2, 3, 4\}$.

\subsection*{The role of $\sqrt{3}$: cascade-native spatial-dimension count}

The prefactor differences between cascade and de~Sitter rates
involve $\sqrt{3}$ and $\pi$:
\begin{itemize}
\item Share-decay vs.\ Hubble rate: factor $\sqrt{3}$.
\item Total rate vs.\ horizon flux: factor $\pi/(6\sqrt{3})$.
\item Cascade decay rate vs.\ $T_{\mathrm{dS}}$: factor
$2\pi\sqrt{3}$.
\end{itemize}
The $\sqrt{3}$ enters via the Friedmann equation
$H^2 = \Lambda/3$. Tracing this back to its origin: the factor $3$
in Friedmann is the trace of the spatial part of the Einstein
tensor on a maximally-symmetric vacuum, with each of the three
spatial directions contributing $H^2$ to the Friedmann constraint:
\[
3 H^2 \;=\; \underbrace{H^2}_{x} + \underbrace{H^2}_{y} + \underbrace{H^2}_{z} \;=\; \Lambda.
\]
The $3$ is the spatial-dimension count: $d_{\mathrm{space}} = 3$ in
$4 = 1 + 3$ spacetime.

\textbf{This is the cascade's primordial era directly.} The cascade's
foundational era is $\{1, 2, 3, 4\}$ --- four cumulated states. The
slicing recurrence picks one direction as the cascade's resolution
direction (time). The remaining three are spatial. So the cascade's
own structure furnishes exactly $1 + 3$ at the primordial level, with
$d_{\mathrm{space}} = 3$ matching the Friedmann factor exactly.

\begin{observation}[The cascade's $\sqrt{3}$ is the spatial-dimension count]\label{obs:sqrt3-spatial}
The factor $\sqrt{3}$ that appears in the Hubble-rate / share-decay
ratio is $\sqrt{d_{\mathrm{space}}}$ where $d_{\mathrm{space}} = 3$
is the spatial-dimension count of the cascade's primordial era after
the slicing direction is identified as time. The cascade does not
need to derive $\sqrt{3}$ from a deeper structure in the prime tower
or from a higher distinguished dimension --- it sits directly in
the four-cumulated-states structure $\{1, 2, 3, 4\}$, with three
non-slicing axes.
\end{observation}

Working spatially-only at the cascade level, the divisor-weighted
spatial content grows at exactly three content-units per Planck tick
(one per spatial axis):
\[
\frac{dC_{\mathrm{space}}}{dN} \;=\; \frac{d}{dN}\!\!\left[\,\sum_{d \in \{2,3,4\}} d \cdot \lfloor N/d \rfloor\right] \;=\; 1 + 1 + 1 \;=\; 3.
\]
This is the cascade-native trace over three spatial axes, structurally
identical to the trace giving the $3$ in Friedmann. The square root,
$\sqrt{3}$, appears whenever one converts a trace (total) to a
per-direction quantity.

\textbf{Implication for the rate match.} If one compares cascade
share-decay rate to de~Sitter Hubble rate \emph{both as per-direction
quantities}, the $\sqrt{3}$ does not appear: the cascade's
per-spatial-dimension share-decay rate is $1/N$, and the de~Sitter
per-direction Hubble rate is $H = \sqrt{\Lambda/3}$, both
$\propto 1/N$ at the saturation epoch. The $\sqrt{3}$ in the earlier
comparison arose from comparing cascade-total to de~Sitter-per-direction
--- a trace-vs-per-direction mismatch in the identification, not
a genuine prefactor gap.

The Hawking-analog rate match is therefore tighter than the bare
$\sqrt{3}$-ratio suggests. The cascade's spatial structure already
contains the Friedmann factor; the apparent prefactor is the cascade
counting its own spatial dimensions, not a residual mismatch with
standard cosmology.

\subsection*{What this gives}

The cascade's prime/composite recursion produces a Hawking-radiation-
like dynamic without invoking semiclassical gravity:

\begin{itemize}
\item \emph{Mechanism}: spacetime's share of total cascade content
decays as $1/N$ at every Planck tick.
\item \emph{Rate}: cascade-native, with e-folding time exactly $N$
Planck ticks (the cosmic age itself).
\item \emph{Direction of flow}: spacetime $\to$ higher-prime cascade
sector.
\item \emph{Match to de~Sitter}: functional forms agree
($\propto 1/N$ for decay, $\propto N$ for absolute rates). The
$\sqrt{3}$ that appears in trace-vs-per-direction comparisons is
cascade-native: it is $\sqrt{d_{\mathrm{space}}}$ where
$d_{\mathrm{space}} = 3$ is the spatial-dimension count of the
primordial era after the slicing direction is identified as time
(Observation~\ref{obs:sqrt3-spatial}). When compared
per-spatial-direction on both sides, the $\sqrt{3}$ disappears.
\end{itemize}

This is the closest the cascade has come to a direct (Friedmann-
bypassing) derivation of de~Sitter cosmology. The cascade's
structural dynamics --- spacetime content $\sim 4N$, total $\Sigma
\sim N^2$, share $\sim 1/N$, with the spatial sector $C_{\mathrm{space}}
\sim 3N$ separately --- produce de~Sitter-like rates that match
observation, with the apparent $\sqrt{3}$ prefactor identified as
the cascade's own spatial-dimension count rather than an external
Friedmann projection.

The remaining $\pi$-rational prefactors are cascade-natural and
individually derived; whether they can be unified under a single
cascade-action identity (closing the rate match into a forced
identity rather than a per-route order-unity convergence) is open.

% ============================================================
\section{What This Paper Does Not Do}\label{sec:does-not-do}
% ============================================================

\textbf{Does not derive the distinguished dimensions.} The set
$\{5, 7, 19, 217\}$ remains derived in \xref{part0}{thm:tower} from
Gamma-function critical points on the integer axis. This paper
observes that the first three are atomic events (primes) and the
fourth is a derived event (the composite $7 \cdot 31$), and that the
super-prime count at $N = 217$ equals $19$ (the previous distinguished
atomic event), but neither observation amounts to a prime-cascade
derivation. The Gamma-function derivation remains load-bearing.

\textbf{Does not derive spacetime stability at $d = 4$.} The formal
derivation of $d = 4$ as the observer's spacetime is in
\xref{part3}{paperdoc} (Lovelock uniqueness intersected with
Clifford periodicity). This paper observes that $d = 4$ is the
cascade's first derived event in the primordial era, and that this is
consistent with $d = 4$ being the boundary of the cascade's
foundational era before the first distinguished dimension. This is a
structural consistency observation, not a derivation.

\textbf{Does not derive $\Lambda$ from prime structure alone.} The
cascade's $\Lambda \sim 10^{-120}$ prediction continues to derive
from the dimensional route through $\Omega_{19} \Omega_{217}$ in
Part~0. The prime-cascade $\Sigma(N)$ matches this value at the
current cosmic depth, but the paper does not establish that
$\Sigma(N)^{-1}$ \emph{is} the cascade's $\Lambda$ as a function of
$N$. That identification is open.

\textbf{Does not derive the three-way match as an identity.} The
three cascade-internal scales of
Section~\ref{sec:holographic-match} ($\Sigma(N)$,
$S_{\mathrm{cosmic}} = \Omega_7/(4\Omega_{217})$, $\Lambda^{-1}$)
agree at order $10^{120}$ with explicit $\pi$-rational prefactors,
but the prefactors do not coincide exactly. $\Sigma$ and
$S_{\mathrm{cosmic}}$ differ by a factor of about $3$ at the current
$N$; the saturation epoch $N_{\mathrm{sat}}$ where they match
exactly is at $\approx 1.7 \times 10^{60}$, within a factor of about
$2$ of the current cosmic depth. The relation between
$\Lambda^{-1}$ and $S_{\mathrm{cosmic}}$ is mediated by the Gram
correction in Part~0's $\Lambda$ formula. None of these prefactor
relations is forced from austerity alone; the paper does not derive
an exact identity locking the three cascade routes together.

\textbf{Does not derive the saturation epoch.} Whether the cascade's
preferred resolution depth (the cosmic age in Planck ticks) sits at
the saturation point as a cascade-internal forced consequence, or
whether it is anthropically selected by observer existence, is open.
The paper observes the convergence; it does not derive its timing.

\textbf{Inherits the asserted step of Paper~II\,=\,III.} The
cascade-native horizon entropy formula $S = A/d$ assumes that the
content--area map is linear with slope $1/d$ for any cascade area,
not only for $A = \Omega_{d-1}$ at the unit sphere
(\xref{part23}{rem:sp23-status}). This paper uses the formula at
$A = \Omega_7/\Omega_{217}$ (the cosmic-horizon area) inheriting the
same assertion. A cascade-action derivation that forces the linear
content--area map would close this gap simultaneously for
Paper~II\,=\,III and the present paper.

\textbf{Does not derive every prefactor in the Hawking-analog
rate-match from a single cascade-action identity.}
Section~\ref{sec:hawking-analog} shows that the cascade's
spacetime-share decay rate matches de~Sitter Hubble dilution in
functional form ($\propto 1/N$, both with e-folding time linear in
cosmic age). The $\sqrt{3}$ that appears in some comparisons is
cascade-native --- it is the spatial-dimension count of the cascade's
primordial era (Observation~\ref{obs:sqrt3-spatial}) --- and
disappears when both sides are compared per-spatial-direction.
Remaining $\pi$-rational prefactors ($\pi^2/6, \pi/(6\sqrt{3})$,
$2\pi\sqrt{3}$) are cascade-natural and individually derived, but
not yet united under a single cascade-action identity. Whether they
can be so unified, or whether they represent irreducible signature
of the cascade's projection to standard cosmology, is open.

\textbf{Does not commit to a single reading without remainder.}
The three-way match of Section~\ref{sec:holographic-match}
disfavours reading~(a) of Section~\ref{sec:bridge-part0} on
observational grounds (cosmic acceleration) and develops reading~(c)
with a specific mechanism (cascade saturation at the cosmic horizon
scale, with a Hawking-radiation dynamic at
Section~\ref{sec:hawking-analog}). Reading~(b) (numerical
coincidence at the current $N$) remains a possible alternative if
the matches turn out to be approximate rather than structurally
forced.

% ============================================================
\section{Open Questions}\label{sec:open-questions}
% ============================================================

\textbf{Three-way match: forced or coincident?} The agreement at
order $10^{120}$ between the prime-cascade $\Sigma(N) \approx 0.82
\times 10^{120}$, the cascade hidden geometry
$S_{\mathrm{cosmic}} = \Omega_7/(4\Omega_{217}) \approx 2.5 \times
10^{120}$, and the dimensional inverse $\Lambda^{-1} \approx
10^{120}$ at the current cosmic depth is striking
(Section~\ref{sec:holographic-match}). Whether the cascade's
distinguished dimensions and the cascade's preferred resolution depth
$N$ are locked together by an austerity-forced relation, or whether
the saturation epoch where these three agree is anthropically
selected as the cosmic moment when observers exist, is the principal
open question. A cascade-internal derivation that forces the
saturation epoch would constitute a structural explanation of the
Dirac large-number coincidence.

\textbf{Hawking-analog rate match.}
Section~\ref{sec:hawking-analog} shows that the cascade's
spacetime-share decay rate is $1/N$ exactly, with e-folding time
the cosmic age itself. Comparing total cascade rate to per-direction
de~Sitter Hubble rate gives a $\sqrt{3}$ ratio; comparing
per-direction to per-direction (using the cascade's spatial sector
$d \in \{2, 3, 4\}$ explicitly) removes the $\sqrt{3}$
(Observation~\ref{obs:sqrt3-spatial}). The $\sqrt{3}$ is the cascade's
own spatial-dimension count, not a residual prefactor mismatch. What
remains genuinely open is the $\pi$-prefactor structure: the absolute
prefactor matches between cascade and de~Sitter rates involve
$\pi^2/6, \pi/(6\sqrt{3})$, etc., which are cascade-natural but not
yet derived from a single cascade-action identity. A cascade-native
derivation bypassing Friedmann --- starting from the slicing
recurrence and producing de~Sitter directly --- would close these
remaining prefactor identities.

\textbf{Prefactor identity (refined).} The naive identity
$\Lambda(N) \stackrel{?}{=} 12/(\pi^2 N^2)$ is observationally
disfavoured: it requires $\Lambda$ to vary as $1/N^2$ and produces
power-law cosmology $a(t) \propto t^{2/\pi}$, decelerating, against
the observation of late-time cosmic acceleration. A refined
prefactor identity at the saturation epoch, holding
$\Sigma(N_{\mathrm{sat}}) = S_{\mathrm{cosmic}}$, would give
$N_{\mathrm{sat}}^2 = 3\,\Omega_7/(\pi^2\, \Omega_{217})$ and force a
specific relationship between the cascade's distinguished dimensions
and the cascade's resolution depth at observer existence. Whether
this identity is exact (with the order-unity factor of $\sim 2$
between $N_{\mathrm{sat}}$ and the actual current $N$ absorbed in
the Gram correction or in the cascade's primordial-era detailing) or
approximate (with an irreducible factor of $\sim 2$ representing
genuine slack) is open.

\textbf{Time-variation of $\Lambda$.} Under the saturation reading
$\Lambda$ stays at the Part~0 dimensional value $\sim 10^{-120}$,
consistent with the observed cosmological constant. The cascade's
$\Sigma(N)$ grows quadratically but the cascade's hidden geometry
$S_{\mathrm{cosmic}}$ is a fixed cascade ratio. The cascade does not
predict observable time-variation of $\Lambda$ in this reading.

\textbf{Generative derivation of distinguished dimensions.} A
prime-structural derivation of $\{5, 7, 19, 217\}$ from austerity
alone (without going through Gamma-function critical points) would
turn the prime cascade from a structural / bridging reading into a
generative cascade. Whether this is achievable is open. The
observation that the first three distinguished dimensions are atomic
events and the fourth is a derived event with $19$ super primes at
its tick is suggestive but not yet a derivation.

\textbf{Closing the asserted step.} Paper~II\,=\,III's
$S = A/d$ depends on an asserted step
(\xref{part23}{rem:sp23-status}) about scale-invariance of the
content--area map. Closing this step --- via a cascade-action
derivation that forces the linear content--area map for any cascade
area --- would tighten the three-way match of
Section~\ref{sec:holographic-match} into a forced identity rather
than an order-of-magnitude convergence with cascade-natural
prefactors.

\textbf{Connection to standard holographic principles.} The
cascade's horizon entropy $S = A/d$ (Paper~II\,=\,III) recovers the
Bekenstein--Hawking $S = A/4$ at the observer's dimension $d = 4$
via boundary dominance, without invoking semiclassical gravity. The
three-way match of Section~\ref{sec:holographic-match} extends this
identification to the cosmic horizon, where the cascade's hidden
geometry $\Omega_7/(4\Omega_{217})$ equals the inverse cosmological
constant from Part~0 within cascade-natural prefactors. Sharpening
the match into a forced identity would offer a number-theoretic
underpinning for the cosmological holographic principle. This is
outside the scope of the present paper but is the natural next step
in relating the prime cascade to standard cosmological information
theory.

% ============================================================
\section{Foundational Commitments}\label{sec:foundational-commitments}
% ============================================================

This paper inherits the Prelude's foundational footprint without
modification:

\textbf{Sole pre-mathematical input.} Definition~$0 \neq 1$
(\xref{prelude}{def:axiom}).

\textbf{Foundational footprint.} Pure $\mathrm{RCA}_0$: natural
numbers, $\Sigma^0_1$ induction, $\Delta^0_1$ comprehension,
computable functions on $\mathbb{N}$, computable reals. The Monotone
Convergence Theorem and stronger analytic principles are not
foundational commitments; they appear in this paper only as
bookkeeping shorthand when stating asymptotic results.

\textbf{Analytic content as computable processes.} The chain-count
asymptotics, $\Sigma(N) \sim (\pi^2/12) N^2$, and the divisor-density
results are all $\mathrm{RCA}_0$-expressible convergence statements
about computable sequences; no completed real is named at the limit.
At any finite resolution depth $N$ every quantity in this paper has
a specific rational value.

\textbf{Downstream bookkeeping.} The closed-form notation
(asymptotic relations, integral identities) is shorthand for
finite-$N$ statements on computable sequences, consistent with the
Prelude's framing.

\bigskip

\begin{thebibliography}{99}

\bibitem{prelude} RTAC, \extlink{\cascadebase/cascade-series-prelude.pdf}{\textit{The Cascade Series --- Prelude: Why Nothing Has Structure}} (2026).

\bibitem{part0} RTAC, \extlink{\cascadebase/cascade-series-part0.pdf}{\textit{The Cascade Series --- Part 0: The Tower}} (2026).

\bibitem{part23} RTAC, \extlink{\cascadebase/cascade-series-part2-equals-3.pdf}{\textit{The Cascade Series --- Part II\,=\,III: Quantum Gravity without Quantising Gravity}} (2026).

\bibitem{part3} RTAC, \extlink{\cascadebase/cascade-series-part3.pdf}{\textit{The Cascade Series --- Part III: General Relativity from Lovelock and Clifford}} (2026).

\end{thebibliography}

\end{document}
